{
	"name": "<%= _.slugify(appname) %>",
	"version": "0.0.0",
	"dependencies": { <%
		if (compassBootstrap) { %>
			"bootstrap-sass": "~3.3.4", <%
		} %>
		<%
		if (!compassBootstrap) { %>
		"bootstrap-sass": "~3.3.4", <%
		} %>
		<%
		if (includeRequireJS) { %>
			"requirejs": "^2.1.8", <%
		} %> <%
		if (includeModernizr) { %>
			"modernizr": "^2.6.2", <%
		} %>
		"jquery": "^1.10.2",
		"lt-ie-9": "~0.1.0",
		"cookieconsent2": "~1.0.4"
		},
	"devDependencies": {}
}
